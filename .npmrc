# Supply-chain defense: block npm package versions published in the last 7 days.
# 10080 minutes = 7 days. Mirrors the user's global pnpm config; committed so the
# gate also applies in CI and for other contributors.
minimum-release-age=10080
